development:
  adapter: async
  # adapter: redis

test:
  # adapter: test
  adapter: redis

production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
  channel_prefix: phase_4_project_guidelines_production
